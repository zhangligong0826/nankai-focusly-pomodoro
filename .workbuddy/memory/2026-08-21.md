# 2026-08-21 工作日志

## Focusly UI 优化落地（07:01-07:30）

### 完成内容
按昨日 UI 评审报告（ui-review-report.md）完成 P0+P1+P2 全部 18 项修改，24 个源文件。
改动清单见 .workbuddy/artifacts/ui-fix-changelog.md。

### 关键改动
- viewport 解除禁缩放（WCAG 1.4.4）
- 对比度 token 重构：text-tertiary #999→#6e6e6e；新增 -text 语义文字档 + --color-error + --color-action + --color-primary-text；实测 27 组 0 FAIL
- 图表暗色适配：Weekly/Monthly 硬编码→运行时 cssVar + 主题监听重渲；三图表加 role=img
- iOS 安全区 env(safe-area-inset-bottom)
- 页面/卡片分层（body 用 bg-secondary）
- danger 按钮改描边样式（与主 CTA 形状区分）
- 44px 触控目标（pointer:coarse 媒体查询，PC 保持紧凑）
- 任务整行可点编辑；勾选弹入动画；长按改仅触屏+3s 进度条；select 补下拉箭头；Toast 关闭按钮
- 计时字号 clamp(40px,13vw,56px)；.tnum 等宽数字
- reduced-motion 全局适配；Tab/Switch ARIA 补全；label for 关联
- 新增 THEME.SYSTEM 跟随系统（ThemeToggle 三态选择器），默认 theme=system
- 暗色底中性化 #1c1c22 系（去蓝调）
- TaskView 分组空态统一 EmptyState

### 验证
- 构建 0 错误；单测 43/43；type-check exit 0

### 设计约定（重要，后续开发遵守）
- --color-primary (#e74c3c) 仅图形/大字号；文字场景一律 --color-primary-text
- 语义色文字必须用 -text 变体（warning-text/success-text/info-text）
- 触控目标 --touch-target-min:44px；PC 紧凑用 (hover:hover) and (pointer:fine) 收缩

### 未提交
代码尚未 git commit，等用户确认后提交。

## Focusly 竞品分析与改进方向调研（08:13-08:20）

### 完成内容
以「设计原型专家团」主理人身份，走正式团队协作流程（TeamCreate + 2 位研究成员并行），产出竞品分析报告 .workbuddy/artifacts/competitive-analysis-report.md。

### 调研方式
- 现状盘点：主理人基于全量代码审阅
- 竞品：Forest/Focusmate/番茄ToDo/潮汐/pomofocus/Flipd/Session 等（联网）
- 开源：pomofocus 重构版/super-productivity/Pomatez 等（联网）

### 核心发现
1. **最关键的 bug**：计时用 setInterval 递减（stores/timer.js:137），后台标签页节流会漂移、刷新即丢进度 → 最高优先级修复项
2. 竞品 8 条共性趋势：养成可视化/损失厌恶/社会监督/锁机分级/声音场景化/数据复盘/意图复盘/任务估时
3. 开源 8 条工程模式：deadline 计时/刷新恢复/存储分层/状态机/PWA/Web Audio/导出/测试金字塔

### 改进方向优先级
- P0：①计时器 deadline 化+刷新恢复 ②IndexedDB 分层
- P1：①养成物 ②热力图 ③白噪音场景化 ④锁机分级 ⑤意图复盘 ⑥任务估时
- P2：①轻社交 ②云同步 ③测试金字塔 ④通知去重

### 建议路线
先地基（P0 计时正确性）→ 差异化（P1 养成物+数据洞察）→ 社交云端（P2）

## P0-1 计时器 deadline 化 + 刷新恢复落地（08:23-08:31）

### 完成内容
修复核心计时 bug：setInterval 递减 → 绝对截止时间戳模式。

### 改动文件
- `src/utils/timer.js`：新增 `computeRemaining(deadlineAt, now)` / `computeDeadline(seconds, now)` 纯函数，保留 advanceCountdown 兼容旧接口
- `src/stores/timer.js`：模块级 deadlineAt 变量；start/resume 用 computeDeadline，tick 用 computeRemaining 重算；pause 先校准再停；新增 persistState/clearPersistedState/init 恢复逻辑
- `src/utils/constants.js`：新增 LS_KEY.TIMER_STATE
- `test/timer.test.mjs`：43→47 用例，补 computeRemaining/computeDeadline 边界与互逆测试

### 恢复逻辑
- 运行中→按 deadline 重算，已过期自动补结算 complete(false)
- 暂停中→保持暂停与剩余秒数
- 无状态→回默认 focus/idle

### 验证
- npm test 47/47 通过；build 0 错误；type-check exit 0
- 浏览器实测：开始计时 25:00→3 秒后 24:57；刷新后 24:50（未归零，恢复成功）；暂停 24:36 刷新后仍 24:36 且按钮为「继续」

### 待办
- P0-2 IndexedDB 存储分层（需用户确认权衡后实施）

## P0-2 IndexedDB 存储分层落地（08:31-08:40）

### 完成内容
大块历史数据（打卡 checkins、会话 sessions）从 localStorage 迁入 IndexedDB，突破 5MB 上限；设置/任务/状态等小数据留 localStorage。

### 改动文件
- 新增 `src/utils/indexedDB.js`：原生 IndexedDB Promise 封装（openDB/get/set/remove），SSR/测试环境安全，失败降级
- 新增 `src/utils/migrate.js`：一次性幂等迁移（LS 旧数据→IndexedDB→删旧 key）
- `src/stores/checkin.js`：checkins 读写改 idbGet/idbSet
- `src/stores/stats.js`：loadRealCheckins 异步化，三处调用加 await
- `src/stores/timer.js`：sessions 读写改 idbGet/idbSet
- `src/utils/export.js`：exportJSON/exportCSV 异步化，checkins/sessions 从 IDB 读
- `src/components/settings/DataExportPanel.vue`：导入时 checkins/sessions 写 IDB
- `src/main.js`：store init 前 await migrateLegacyData()

### 存储分层策略
- IndexedDB（库 focusly / store kv）：CHECKINS、SESSIONS
- localStorage：TIMER_CONFIG、TASKS、SETTINGS、CURRENT_TASK、TIMER_STATE、THEME、celebrated 等小数据

### 验证
- build 0 错误；type-check exit 0；test 47/47
- 浏览器实测：跳过计时后 localStorage 的 checkins/sessions 为 null，IndexedDB 中 sessions count=1；统计页正常读取

### 注意
- 迁移幂等：LS 已无旧 key 不会重复迁移
- IndexedDB 不支持结构化克隆的类（如 Date）会降级，当前数据均为纯对象/数组，无影响

## P0-P5 六级路线图（08:39）

### 完成内容
将改进方向细化为 P0-P5 六级优先级路线图，产出 .workbuddy/artifacts/roadmap-p0-p5.md。

### 档位划分
- P0 地基（✅已完成）：计时器 deadline 化 + IndexedDB 分层
- P1 差异化：养成物/热力图/意图复盘/任务估时/白噪音场景化/锁机分级
- P2 数据：多级报告/高效时段/周报PDF/通知去重
- P3 社交：自习室/好友榜/分享卡片
- P4 工程债：测试金字塔/TS迁移/架构文档+CI
- P5 平台：云同步/多端/主题市场

### P1 建议先做顺序
P1-3 意图复盘(低) → P1-4 任务估时(低) → P1-5 白噪音(中低) → P1-2 热力图(中) → P1-1 养成物(高) → P1-6 锁机分级(中)

## P1 前三项落地（09:23-09:40）

### 完成内容
实现 P1 差异化功能前三项（成本最低、差异化最明显）。

### P1-3 意图设定 + 结束反思闭环
- timer store 新增 intent / pendingReflectionId 状态；start(focusIntent) 记录意图；complete() 写 intent 到 session 并触发反思
- 新增 saveReflection(rating, text) / dismissReflection()
- 新增 IntentDialog.vue（开始前弹意图输入）、ReflectionDialog.vue（完成后弹 1-5 星打分+反思）
- TimerControls 开始按钮先弹意图；App.vue 全局挂载 ReflectionDialog

### P1-4 任务估时 + 番茄绑定强化
- TaskItem 新增 estimateMinutes（剩余番茄 × 单番茄时长），显示「⏱ 约 X 分钟」
- TimerDisplay 绑定任务时显示「还需约 X」；引入 useSettingsStore

### P1-5 白噪音场景化 + 每日金句
- NOISE_TYPE 扩充 WAVES/FOREST（移除 SILENCE），whiteNoise 单值→数组多选混音
- useSound 重构为多场景 Map 混音（海浪用正弦包络、森林用噪声+鸟鸣脉冲）
- WhiteNoisePlayer 改多选叠加 + 关闭按钮 + 音量滑块
- 新增 DAILY_QUOTES 10 条 + DailyQuote.vue 按日期取模轮换
- settings store 兼容旧版 whiteNoise 字符串→数组迁移；types/index.ts 同步

### 验证
- build 0 错误；type-check exit 0；test 47/47
- 浏览器实测：金句显示「今天的专注，是明天的底气。」；意图弹窗弹出→输入→开始；任务估时显示「约 2 小时 5 分钟」等；白噪音多选 rain+waves 后 localStorage whiteNoise=["rain","waves"]、音量滑块出现

### 注意
- skip 不计完成，不触发反思（正确行为）
- 反思弹窗仅正常完成 focus 时触发
- 代码未 git commit

### 待办（P1 剩余）
- P1-2 热力图、P1-1 养成物、P1-6 锁机分级

## P1 剩余三项落地 + git commit（09:43-10:00）

### 完成内容
走 Plan 模式（探索代理×3 + 规划代理 + 用户确认三个决策）后实现 P1 剩余三项。

### P1-6 锁机分级（软提醒/强锁两级）
- constants.js 新增 FOCUS_LOCK_MODE {off/soft/hard}；DEFAULT_SETTINGS.focusLock='off'
- timer.js 新增 abort(reason)：记录 aborted 会话、不打卡/不加番茄/不反思/不庆祝；TIMER_STATE 持久化 focusLock 级别
- 刷新恢复：强锁+deadline 过期→abort 而非 complete（关键修复，防误发打卡）
- useFocusLock.js 判级别：SOFT 弹窗、HARD 直接 abort
- SettingsPanel switch→三态分段选择器；settings store setFocusLockMode + 旧 boolean 兼容

### P1-1 可视化养成物（手绘 SVG）
- constants.js 新增 GROWTH_STAGES（种子/发芽/幼苗/成株/开花，按累计分钟 0/60/180/360/600）、GARDEN_SPECIES（小绿芽/番茄苗streak≥3/向日葵streak≥7）、LS_KEY.GARDEN
- 新增 stores/garden.js：unlocked/plant/harvestedCount/lastStreak/unlockTrigger；reconcile 检测解锁+断签枯萎；wilt/revive
- 新增 components/garden/{GardenPlant,GardenCard,GardenMiniCard,GardenCelebration}.vue（手绘 SVG viewBox 0 0 120 120）
- timer.js：abort→wilt，complete 打卡后→reconcile
- 落点：StatsView StreakBadge 后 GardenCard；TimerView CheckinCard 下 GardenMiniCard；App.vue 挂 GardenCelebration；exportCore 加 garden 备份

### P1-2 日历热力图 + 高效时段
- 新增 utils/statsAggregate.js：computeHeatmap/computePeakHours 纯函数（可测）
- stats.js 加 heatmapData/peakHours + fetchHeatmap/fetchPeakHours（loadRealSessions 读 IndexedDB）
- 新增 HeatmapChart.vue（ECharts Heatmap+Calendar+VisualMap）、PeakHoursChart.vue
- StatsView 新增第 4 Tab「热力图」（热力图+高效时段）

### 验证
- build 0 错误；type-check exit 0；test 51/51（新增 statsAggregate 4 用例）
- 浏览器实测：热力图/高效时段图表渲染、锁机三态选择器、花园卡片+迷你卡（初始"小绿芽·种子"）

### git commit
- 提交 1ebb82d「feat: 完成 P0 计时正确性与 P1 差异化功能」
- 68 文件；截图加入 .gitignore 排除；工作区干净
- 未 push（origin/main 落后 1 个提交）

### 设计约定（新增）
- 养成物成长阈值：种子0/发芽60/幼苗180/成株360/开花600分钟；品种解锁 streak≥3 番茄苗、≥7 向日葵
- abort 与 skip/complete 语义区分：abort 标记 aborted:true，不计入有效专注

## P2 阶段落地 + git push（10:17-10:45）

### 完成内容
走 Plan 模式（探索×2 + 规划代理 + 用户确认三个决策）后实现 P2 四项。

### 用户确认决策
- PDF 周报：html2canvas + jspdf（视觉还原）
- 高效时段：深化为文字建议
- 通知：只做代码级去重

### P2-1 日/周/月/年多级报告
- statsAggregate.js 新增 computeDaily/computeYearly 纯函数
- stats store 加 dailyData/yearlyData + fetchDaily/fetchYearly
- 新增 DailyReport.vue（今日指标+任务分布）、YearlyChart.vue（12 月柱状图）
- StatsView Tab 增至 6（日报/本周/本月/年报/分类占比/热力图），chart-tabs 加 flex-wrap

### P2-2 高效时段建议
- statsAggregate 加 buildPeakAdvice；fetchPeakHours 赋值后生成 peakAdvice
- PeakHoursChart 加建议文字卡

### P2-4 通知去重
- useNotification.js 模块级 lastNotif + 3 秒窗口去重

### P2-3 PDF 周报导出
- 依赖 html2canvas@1.4.1 + jspdf@2.5.2（动态 import，vite manualChunks 加 pdf 分包）
- 新增 WeeklyReport.vue（隐藏 A4 模板，字面量颜色）、pdfExport.js（截图→切片→多页 PDF）
- StatsView 加「导出周报」按钮

### 验证
- build 0 错误；type-check exit 0；test 54/54（新增 computeDaily/computeYearly/buildPeakAdvice 3 用例）
- 浏览器实测：6 Tab 全显示、日报空态提示、年报柱状图、PDF 导出成功（85KB 有效 PDF，JPEG 1588×1436）

### git commit + push
- 提交 fe16736「feat: 完成 P2 数据复盘与导出增强」并 push
- 之前 1ebb82d（P0+P1）也已 push；origin/main 同步

### 技术要点
- html2canvas 不支持 CSS 变量，PDF 模板用字面量 hex
- 中文经 html2canvas 栅格化嵌入，绕开 jspdf 字体嵌入
- PDF 分页：pageHpx = canvas.width*(297/210)，按比例切片



